---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: plausible
  namespace: analytics
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`plausible.65.21.101.130.nip.io`)
      kind: Rule
      services:
        - name: plausible
          port: 8000
  tls:
    certResolver: le
