---
apiVersion: v1
kind: ConfigMap
metadata:
  name: plausible-conf
  namespace: analytics
data:
  # Add your configuration key-value pairs here
  DISABLE_REGISTRATION: invite_only
  BASE_URL: http://testing.midka.dev/
  MAILER_EMAIL: plausible@midka.dev
  SMTP_HOST_ADDR: mail.qdi.fi
  SMTP_HOST_PORT: '587'
  CLICKHOUSE_DATABASE_URL: http://plausible-events-db:8123/plausible
  DATABASE_URL: http://postgres:postgres@plausible-db:5432/plausible
